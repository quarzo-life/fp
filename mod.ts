/**
 * Functional programming helpers for JavaScript and TypeScript.
 *
 * Every function is pure and side-effect free, so consumers can bundle
 * exactly what they use.
 *
 * @example Quick start
 * ```ts
 * import { identity } from "@quarzo-life/fp";
 *
 * identity(42); // 42
 * ```
 *
 * @module
 */
export * from "api/index.ts";
